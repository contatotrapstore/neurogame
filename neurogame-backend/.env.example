# ========================================
# NEUROGAME BACKEND - VARIÁVEIS DE AMBIENTE
# ========================================
#
# IMPORTANTE:
# - Para desenvolvimento local, renomeie para .env
# - Para produção (Railway), configure no dashboard
# - NUNCA commite arquivo .env com valores reais!
# ========================================

# Environment
NODE_ENV=production
PORT=3000

# Supabase (OBRIGATÓRIO)
# Obtenha em: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua_chave_publica_anon_aqui
SUPABASE_SERVICE_KEY=sua_chave_service_role_aqui

# JWT (OBRIGATÓRIO - Use chaves fortes!)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua-chave-secreta-jwt-256-bits-forte
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=sua-chave-secreta-refresh-256-bits-diferente
JWT_REFRESH_EXPIRES_IN=7d

# Asaas - Gateway de Pagamento (OBRIGATÓRIO para produção)
# Cadastre-se em: https://www.asaas.com
ASAAS_API_KEY=sua_chave_api_asaas
ASAAS_WEBHOOK_SECRET=seu_webhook_secret_asaas
ASAAS_ENVIRONMENT=sandbox
# Para produção, mude para: ASAAS_ENVIRONMENT=production

# Plano de Assinatura
SUBSCRIPTION_VALUE=149.90
SUBSCRIPTION_CURRENCY=BRL

# Limites de Dispositivos e Sessão
MAX_DEVICES_PER_USER=3
HEARTBEAT_INTERVAL_HOURS=12
OFFLINE_GRACE_PERIOD_HOURS=48

# CORS - Origens Permitidas (IMPORTANTE!)
# Desenvolvimento:
# CORS_ORIGIN=http://localhost:5173,http://localhost:5174
#
# Produção (adicione URLs do seu admin e launcher):
CORS_ORIGIN=https://seu-admin.vercel.app,https://admin.neurogame.com.br

# ALLOWED_ORIGINS (alternativa - mesma função que CORS_ORIGIN)
ALLOWED_ORIGINS=https://seu-admin.vercel.app,https://admin.neurogame.com.br

# Log Level (info, warn, error, debug)
LOG_LEVEL=info
